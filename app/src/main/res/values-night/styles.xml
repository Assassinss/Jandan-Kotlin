<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="DarkToolbarThemeOverlay" parent="Widget.AppCompat.ActionBar">
        <item name="android:colorControlNormal">@color/title_text_color</item>
        <item name="titleTextColor">@color/gray_600</item>
    </style>

    <style name="DarkToolbarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Dark"/>

</resources>